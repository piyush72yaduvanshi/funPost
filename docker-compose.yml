services:
  db:
    image: postgres:17-alpine
    container_name: practice_db
    environment:
      - POSTGRES_DB=pron
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=piyush73
    ports:
      - "5432:5432"